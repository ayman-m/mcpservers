# ================================
# MCP SERVER (osiris-standalone)
# Matches environment variables in docker-compose.yml
# ================================

# Cortex XSIAM PAPI credentials (required by the MCP server)
CORTEX_MCP_PAPI_URL=https://api-your-tenant.xdr.region.paloaltonetworks.com
CORTEX_MCP_PAPI_AUTH_HEADER=your_papi_auth_header
CORTEX_MCP_PAPI_AUTH_ID=your_papi_auth_id

# MCP transport configuration (used by the server container)
MCP_TRANSPORT=streamable-http
MCP_HOST=0.0.0.0
MCP_PORT=9020
MCP_PATH=/api/v1/stream/mcp

# MCP server auth (bearer token clients must present)
MCP_AUTH_TOKEN=your_mcp_bearer_token

# SSL/TLS (PEM content on one line; use \\n for newlines)
SSL_CERT_PEM=-----BEGIN CERTIFICATE-----\\n...\\n-----END CERTIFICATE-----
SSL_KEY_PEM=-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----

# Logging and XSIAM settings for the server
LOG_FILE_PATH=/app/logs/mcp.json
PLAYGROUND_ID=your_playground_id
SLACK_BOT_TOKEN=your_slack_bot_token

# ================================
# STREAMLIT AGENT (eset-streamlit)
# Matches environment variables in docker-compose.yml
# ================================

# MCP client endpoint (full URL to the server's streamable HTTP endpoint)
MCP_URL=https://mcp-host:9020/api/v1/stream/mcp

# MCP auth presented by the agent to the MCP server
# (decoupled from MCP_AUTH_TOKEN used by the server)
MCP_TOKEN=your_mcp_bearer_token

# Gemini / Vertex AI configuration
GEMINI_API_KEY=your_gemini_api_key
GOOGLE_APPLICATION_CREDENTIALS=content_of_service_account.json
GEMINI_MODEL=gemini-3-pro-preview

# Optional: UI authentication for Streamlit
UI_USER=username
UI_PASSWORD=strong_password

# Optional: reuse SSL PEMs for the Streamlit UI (one-line format)
SSL_CERT_PEM=-----BEGIN CERTIFICATE-----\\n...\\n-----END CERTIFICATE-----
SSL_KEY_PEM=-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----
